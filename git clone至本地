
如何将GitHub上的文件down到本地
1.找到需要down的库的ssh
2.在本地terminal的bash输入命令：git clone ssh
     举例：git clone git@github.com:facebookresearch/mbrl-lib.git
     此处，git@github.com:facebookresearch/mbrl-lib.git 就是我想要下载的库的ssh地址

如果遇到问题：
fatal: not a git repository (or any parent up to mount point /mnt)
Stopping at filesystem boundary (GIT_DISCOVERY_ACROSS_FILESYSTEM not set).
    则在本地terminal的bash输入命令：git init（将本地库初始化）
    然后可以输入：git status（查看状态）